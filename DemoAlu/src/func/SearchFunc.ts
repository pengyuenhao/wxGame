import { Shape } from "../define/GeometryDefine";
import { StatusEnum } from "peng-field";
import { Async } from "../define/AsyncDefine";

export class SerachFunc {
    public search(shape: Shape, filter?: StatusEnum[],exclude?:StatusEnum[]):Async {
        return null
    }
}